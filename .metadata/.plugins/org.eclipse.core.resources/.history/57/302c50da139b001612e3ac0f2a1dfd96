import static java.lang.Math.*;
import static java.lang.String.*;
public class ProblemSet75 {
	public static void main(String[] args) {
		
		double absOutput, ceilOutput, floorOutput;
		int b = 5, c = 10, maxValue, minValue ;
		
		absOutput = Math.abs(5.3);
		
		ceilOutput = Math.ceil(5.4); 
		
		floorOutput = Math.floor(6.2); 
		
		maxValue = Math.max(5,10); 
		
		minValue = Math.min(3,4);
		
		double lengthOutput;
		String indexOf;
		
		indexOf = String.indexOf(2); 
		 
		lengthOutput = String.length("Anish"); 
		 
		isemptyOutput = String.isEmpty();
		 
		String.substring(int beginIndex, int endIndex);
		 
		String.replace(char oldChar, char newChar);
		
		
		
		
		
		
		

	}

}
